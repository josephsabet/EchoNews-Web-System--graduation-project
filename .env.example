# EchoNews Environment Configuration
# Copy this file to .env and fill in your actual values.
# NEVER commit .env to Git — it's already in .gitignore

DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=
DB_NAME=news
DB_PORT=3307
